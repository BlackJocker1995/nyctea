# Ignore macOS system files
._*
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/

# config.yaml holds the default local simulator paths and is committed so the
# pipeline works out of the box; override per-machine with env vars or by
# editing it locally.
# data/config.yaml  (intentionally tracked — see above)

# RL artefacts (checkpoints, buffer shards, per-config deviation npz)
model/
validation/
csv_data/

# Buffer / lock files
*.lock
*.tmp
*.tmp.npz
